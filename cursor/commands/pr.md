Create a pull request for the current changes.

1. Review staged and unstaged changes with `git diff`
2. Write a clear commit message based on what actually changed
3. Commit and push to the current branch (create the branch if on main)
4. `gh pr create` with a descriptive title and a 3-bullet body
5. Return the PR URL
